{
  "RabbitMq": {
    "HostName": "localhost",
    "Port": "5672",
    "UserName": "guest",
    "Password": "guest"
  },
  "ConnectionStrings": {
    "DockerContainer": "docker run -d --name timescaledb -p 5432:5432 -e POSTGRES_PASSWORD=demo -e POSTGRES_DB=demo -e POSTGRES_USER=demo timescale/timescaledb:latest-pg13",
    "TestContext": "Host=localhost;Database=demo;Username=demo;Password=demo"
  },
  "_Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Orleans": "Warning"
    }
  }
}
